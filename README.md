# NAVIGATOR Telegram Bot

Полнофункциональный Telegram-бот для интеграции с NAVIGATOR/VOCALIS сервером с системой платного доступа.

## Описание

Бот предоставляет доступ к фреймворкам NAVIGATOR и VOCALIS через Telegram с системой управления доступом:
- **Тарифная система**: 100 запросов / 30 дней / 1500 ₽
- **Активация через коды**: поддержка активационных кодов
- **База данных**: SQLite/PostgreSQL для хранения пользователей и лимитов
- **Интеграция с NAVIGATOR**: отправка запросов на MCP-сервер

## Установка и настройка

### 1. Клонирование репозитория

```bash
git clone https://github.com/Elhan1505/navigator-telegram-bot.git
cd navigator-telegram-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и заполните необходимые значения:

- **TELEGRAM_BOT_TOKEN**: Токен вашего бота (получить у [@BotFather](https://t.me/BotFather))
- **NAVIGATOR_SERVER_URL**: URL вашего NAVIGATOR/VOCALIS сервера (без `/process` в конце)
- **NAVIGATOR_FRAMEWORK_NAME**: Название фреймворка (по умолчанию: `navigator_vocalis`)

Пример:
```
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
NAVIGATOR_SERVER_URL=https://your-navigator-server.com
NAVIGATOR_FRAMEWORK_NAME=navigator_vocalis
```

### 4. Запуск бота

```bash
python -m telegram_bot
```

Бот запустится в режиме polling и будет готов к обработке сообщений.

## Деплой на Railway

### Настройка переменных окружения в Railway:

1. Откройте ваш проект в Railway
2. Перейдите в раздел **Variables**
3. Добавьте следующие переменные:
   - `TELEGRAM_BOT_TOKEN`
   - `NAVIGATOR_SERVER_URL`
   - `NAVIGATOR_FRAMEWORK_NAME` (опционально)

### Команда запуска:

Команда запуска настроена в `railway.json`:

```
python -m telegram_bot
```

**Важно:** Убедитесь, что запущена только **одна реплика** сервиса, иначе возможны конфликты getUpdates.

## Использование

### Команды бота

- **`/start [код]`** - Приветствие и активация доступа
  - Без кода: показывает текущий статус доступа
  - С кодом: активирует/продлевает доступ на 100 запросов и 30 дней

- **`/profile`** - Показывает профиль пользователя:
  - Статус доступа (активен/неактивен)
  - Количество запросов (доступно/использовано/осталось)
  - Срок действия доступа
  - Общее количество запросов за всё время

- **`/new_dialog`** - Начать новый диалог

### Клавиатура

Бот предоставляет клавиатуру с кнопками:
- **👤 Мой профиль** - быстрый доступ к профилю
- **🔄 Новый диалог** - начать новый диалог

### Отправка запросов

1. Убедитесь, что у вас активирован доступ (команда `/profile`)
2. Отправьте любое текстовое сообщение
3. Бот проверит лимиты и отправит запрос на NAVIGATOR сервер
4. Вы получите ответ с результатом обработки

## Демо-код для теста

При каждом запуске бота автоматически обеспечивается доступность демо-кода для тестирования:

- **Код активации:** `DEMO100`
- **Параметры:** 100 запросов / 30 дней (как основной тариф)
- **Автоматический сброс:** При каждом рестарте бота код возвращается в свежее состояние
- **Многократное использование:** Можно активировать повторно после рестарта бота

### Как активировать демо-доступ:

1. Отправьте команду боту:
   ```
   /start DEMO100
   ```

2. Проверьте активацию:
   ```
   /profile
   ```
   Вы увидите: 100 доступных запросов, срок действия 30 дней

3. Теперь можете отправлять запросы боту!

### Как работает демо-код:

- **При первом запуске:** Код DEMO100 автоматически создаётся в БД
- **При повторных запусках:**
  - Если код уже использован кем-то → автоматически сбрасывается в свежее состояние
  - Если код не использован → остаётся доступным для активации
- **Идемпотентность:** Многократные рестарты бота не создают дубликаты и не ломают логику

### ⚠️ Важно для продакшена:

**Демо-код предназначен ТОЛЬКО для разработки и тестирования!**

Перед запуском в боевом режиме:
1. Откройте файл `telegram_bot/bot.py`
2. Найдите вызов `ensure_demo_code()` в функции `run_bot()`
3. Закомментируйте или удалите эту строку:
   ```python
   # ensure_demo_code()  # Отключено для продакшена
   ```

Иначе любой пользователь сможет получить бесплатный доступ к боту через `/start DEMO100` после каждого рестарта.

## Структура проекта

```
navigator-telegram-bot/
├── telegram_bot/
│   ├── __init__.py      # Инициализация пакета
│   ├── __main__.py      # Точка входа (python -m telegram_bot)
│   ├── bot.py           # Основной модуль бота с обработчиками
│   ├── models.py        # Модели БД (User, ActivationCode)
│   ├── access.py        # Логика управления доступом и лимитами
│   └── navigator.py     # Интеграция с MCP-сервером NAVIGATOR
├── requirements.txt     # Зависимости Python
├── .env.example        # Пример переменных окружения
├── .gitignore          # Исключаемые файлы
├── railway.json        # Конфигурация для Railway
└── README.md           # Документация
```

## Технические детали

### Эндпоинт NAVIGATOR сервера

Бот отправляет POST запрос на `{NAVIGATOR_SERVER_URL}/process` с телом:

```json
{
  "framework": "navigator_vocalis",
  "input": "текст от пользователя"
}
```

### Ожидаемый формат ответа

```json
{
  "output": "ответ от сервера"
}
```

## Требования

- Python 3.8+
- python-telegram-bot >= 21.0
- httpx >= 0.27.0
- sqlalchemy >= 2.0.0
- psycopg2-binary >= 2.9.0 (опционально, для PostgreSQL)

## Отладка и решение проблем

### Ошибка `telegram.error.Conflict`

**Что означает:**
Эта ошибка возникает, когда несколько экземпляров бота одновременно пытаются получать обновления (getUpdates) с одним и тем же токеном.

**Возможные причины:**

1. **Несколько запущенных экземпляров:**
   - Бот запущен локально и на Railway одновременно
   - Бот запущен в нескольких сервисах на Railway
   - Старый процесс не завершился корректно

2. **Несколько реплик на Railway:**
   - В настройках Railway указано больше одной реплики

3. **Другой процесс использует токен:**
   - Webhook настроен через другой сервис
   - Тестовый инстанс всё ещё работает

**Решение:**

1. **Проверьте количество реплик на Railway:**
   ```
   Settings → Deploy → Replicas: должна быть 1
   ```

2. **Убедитесь, что бот не запущен где-то ещё:**
   - Остановите все локальные инстансы
   - Проверьте другие сервисы/серверы

3. **Очистите pending updates:**
   Бот автоматически использует `drop_pending_updates=True` при запуске

4. **В крайнем случае - используйте /revoke:**
   - Напишите @BotFather
   - Отправьте `/revoke`
   - Выберите вашего бота
   - Затем создайте новый токен через `/token`

**Логирование:**

Бот автоматически логирует детальную информацию о Conflict-ошибке:
```
ОШИБКА: Получен Conflict от Telegram API
Возможные причины:
  1. Бот уже запущен в другом месте
  2. Несколько экземпляров бота на Railway
  3. Старый процесс не завершился корректно
```

После обнаружения Conflict бот корректно завершает работу (exit code 1), чтобы избежать бесконечных рестартов.

### Команда /profile не отвечает или показывает ошибку

**Исправлено в последней версии!** Команда `/profile` теперь работает корректно в следующих случаях:

- **Новый пользователь без активации** - показывает приглашение активировать доступ через `/start КОД`
- **Активный пользователь** - показывает полную статистику: статус, запросы, срок действия
- **Истекший доступ** - показывает информацию о продлении

**Если всё ещё есть проблемы, проверьте:**

1. **База данных инициализирована:**
   - Проверьте логи: должно быть "База данных инициализирована"
   - Убедитесь, что файл `navigator_bot.db` создан (для SQLite)

2. **Доступ к БД:**
   - Для PostgreSQL: проверьте переменную `DATABASE_URL`
   - Проверьте права доступа к файлам

3. **Логи:**
   - Проверьте логи: при успешной работе `/profile` будет "Профиль успешно отправлен"
   - При ошибках будут детальные сообщения с указанием причины

### Бот не отвечает на сообщения

**Проверьте:**

1. **Переменные окружения:**
   - `TELEGRAM_BOT_TOKEN` - правильный ли токен
   - `NAVIGATOR_SERVER_URL` - доступен ли сервер

2. **Доступ активирован:**
   - Проверьте `/profile` - должен показывать активный статус
   - Если доступ не активирован - используйте `/start КОД`

3. **Логи NAVIGATOR сервера:**
   - Проверьте, доходят ли запросы до NAVIGATOR сервера
   - Проверьте формат ответа сервера

### Полезные команды для отладки

```bash
# Просмотр логов на Railway
railway logs

# Проверка переменных окружения
railway variables

# Рестарт сервиса
railway restart
```

## Переменные окружения

### Обязательные:
- `TELEGRAM_BOT_TOKEN` - Токен бота от @BotFather
- `NAVIGATOR_SERVER_URL` - URL MCP-сервера NAVIGATOR

### Необязательные:
- `NAVIGATOR_FRAMEWORK_NAME` - Имя фреймворка (по умолчанию: `navigator_vocalis`)
- `DATABASE_URL` - Строка подключения к БД (по умолчанию: SQLite)
- `PAYMENT_LINK` - Ссылка на оплату/активацию (BotHelp или другая система)

## Лицензия

MIT
